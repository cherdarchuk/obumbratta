{
  "name": "obumbratta",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "archie": "node getArchie.js",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "prettier --plugin-search-dir . --check . && eslint .",
    "format": "prettier --plugin-search-dir . --write .",
    "pages:dev": "wrangler pages dev --compatibility-date=2023-10-25 --proxy 5173 -- npm run dev",
    "pages:deploy": "npm run build && wrangler pages deploy .svelte-kit/cloudflare"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^2.0.0",
    "@sveltejs/adapter-cloudflare": "^2.3.3",
    "@sveltejs/kit": "^1.20.4",
    "eslint": "^8.28.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-svelte": "^2.30.0",
    "prettier": "^2.8.0",
    "prettier-plugin-svelte": "^2.10.1",
    "svelte": "^4.0.5",
    "vite": "^4.4.2",
    "wrangler": "^3.15.0"
  },
  "type": "module",
  "dependencies": {
    "archieml": "^0.5.0",
    "json": "^11.0.0",
    "node-fetch": "^3.3.2"
  }
}
